// Base URL of your backend (Node) API.
// Replace this with the live URL when you deploy the backend,
// e.g. "https://your-backend.onrender.com".
const BACKEND_BASE_URL = "http://localhost:4000";

// Default search configuration used on initial load (can be customized).
const DEFAULT_SEARCH_KEYWORD = "best tech gadgets";
const DEFAULT_SEARCH_CATEGORY = "";

